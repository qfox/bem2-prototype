
var BEM = require('bem');

/*BEM.on('ready', function (BEM) {
	console.log('asdasd', BEM);
});*/

//var FS = require('fs'), UTIL = require('util');

/*FS.readdir('node_modules', function (err, files) {
	console.log(files);
	files.forEach(function (submodule) {
		FS.readFile('package.json', 'utf-8', function (err, data) {
			if (err) throw err;
			UTIL.puts(data);
		});
	});
});*/
